USE STUDENTS;

create table students(
            ID INT NOT NULL,
            NAME VARCHAR(20) NOT NULL,
            AGE INT NOT NUL, 
            COURSE CHAR(20) NOT NULL, 
            PRIMARY KEY(ID)
);

DESC STUDENT;